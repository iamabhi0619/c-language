# include <stdio.h>

int main()
{
    int side;
    printf("Enter the side of a square "); //print the info about the input
    scanf("%d", &side);// storing the input value in side

    printf("Area of a Square is %d",side*side);
    /* print the area of a sq with direct calculation 'aysa humlog kr skta hai' */
    return 0;
}